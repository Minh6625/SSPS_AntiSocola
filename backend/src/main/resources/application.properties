# Server Configuration
server.port=${SERVER_PORT:8080}
spring.application.name=${APP_NAME:layered-architecture-backend}

# Database Configuration (SQL Server) - COMMENTED FOR DEMO
# Uncomment khi đã có database và cập nhật .env
# spring.datasource.url=jdbc:sqlserver://${DB_HOST:localhost}:${DB_PORT:1433};databaseName=${DB_NAME};encrypt=true;trustServerCertificate=true
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA/Hibernate Configuration - COMMENTED FOR DEMO
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.app=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# CORS Configuration (cho phép Frontend Next.js kết nối)
# Sẽ cấu hình chi tiết trong WebConfig class
